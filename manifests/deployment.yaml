apiVersion: apps/v1
kind: Deployment
metadata:
  name: hrms-app
  namespace: default
  labels:
    app: hrms-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hrms-app
  template:
    metadata:
      labels:
        app: hrms-app
    spec:
      containers:
      - name: hrms-app
        image: allvysharedacr.azurecr.io/HRMS-Service:$(Build.BuildId)
        imagePullPolicy: Always

        ports:
        - containerPort: 8000
          name: http

        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: hrms-secrets
              key: database-url
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: hrms-secrets
              key: secret-key
        - name: SMTP_USERNAME
          valueFrom:
            secretKeyRef:
              name: hrms-secrets
              key: smtp-username
        - name: SMTP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: hrms-secrets
              key: smtp-password
        - name: FROM_EMAIL
          valueFrom:
            secretKeyRef:
              name: hrms-secrets
              key: from-email

        envFrom:
        - configMapRef:
            name: hrms-config

        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"

        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3

        volumeMounts:
        - name: logs
          mountPath: /app/logs

      volumes:
      - name: logs
        emptyDir: {}

      restartPolicy: Always
