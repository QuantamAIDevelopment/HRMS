apiVersion: v1
kind: Service
metadata:
  name: hrms-service
  namespace: default
  labels:
    app: hrms-service
spec:
  type: LoadBalancer
  selector:
    app: hrms-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
